# ==============================================================================
# Social Media Content Analyzer — Environment Configuration
# ==============================================================================

# Server Port (Default: 5001)
PORT=5001

# Client URL (for CORS in production / split hosting)
CLIENT_URL=http://localhost:3000

# Optional: Google Gemini API Key
# If omitted, the application operates 100% locally with built-in heuristic NLP engine.
# GEMINI_API_KEY=your_gemini_api_key_here
